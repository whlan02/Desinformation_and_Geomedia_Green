# Railway Environment Variables Template
# Copy these to your Railway project's environment variables

# Port (Railway will set this automatically)
PORT=3001

# Node Environment
NODE_ENV=production

# CORS Origins (Update with your actual frontend URLs)
CORS_ORIGINS=https://your-frontend-domain.com,https://geocam-web-frontend.onrender.com,https://geocam-api.onrender.com

# Optional: Memory and Performance Settings
NODE_OPTIONS=--max-old-space-size=2048

# Optional: Logging Level
LOG_LEVEL=info 